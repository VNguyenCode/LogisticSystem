export function processRestock(req, res) {

    //receive restock info through req.body
    //update our catalog with the inventory of each product_id in products table

    //update the quantities of the product in our catalog

    //query all the orders that have products that still has outstanding qty order by DESC order 

    //pass the order into process-order middleware

}